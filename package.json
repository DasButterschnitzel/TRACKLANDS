{
  "name": "tracklands",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "TRACKLANDS: a cozy railway empire builder (Three.js PWA, no build step).",
  "scripts": {
    "test": "node tests/run.mjs",
    "test:quick": "node tests/run.mjs --quick",
    "build:sw": "node tools/build-sw.mjs",
    "serve": "node -e \"import('./tests/lib.mjs').then(async (l) => { const s = await l.startServer(); console.log(s.url); })\""
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
